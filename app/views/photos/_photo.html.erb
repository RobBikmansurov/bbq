<% image_title = "Добавил #{render partial: 'users/user', object: photo.user}" %>

<%= link_to image_tag(photo.photo.thumb.url, class: "img-fluid"),
            photo.photo.url,
            data: {toggle: 'lightbox', title: image_title, gallery: 'gallery'} %>
